@model IEnumerable<DAISY.Models.tb_LOAISANPHAM>

@{
    ViewBag.Title = "LoaiSP";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Loại sản phẩm</h2> 

<p>
    @Html.ActionLink("Thêm mới loại sản phẩm", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TENLOAISANPHAM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HINHANH)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TENLOAISANPHAM)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HINHANH)
            </td>
            <td>
                @Html.ActionLink("Cập nhật", "Edit", new { id = item.IDLOAISANPHAM }) |
                @Html.ActionLink("Xem", "Details", new { id = item.IDLOAISANPHAM }) |
                @Html.ActionLink("Xoá", "Delete", new { id = item.IDLOAISANPHAM }) 
            </td>
        </tr>
    }

</table>
