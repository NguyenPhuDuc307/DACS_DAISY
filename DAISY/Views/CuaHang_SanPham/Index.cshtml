@model IEnumerable<DAISY.Models.tb_CUAHANG_SPCT>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tb_CUAHANG.TENCUAHANG)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tb_KICHCO.TENKICHCO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TENSANPHAM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HINHANH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GIASANPHAM)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.tb_CUAHANG.TENCUAHANG)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.tb_KICHCO.TENKICHCO)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TENSANPHAM)
    </td>
    <td>
        <img src="@item.HINHANH" height="150" width="150" style="object-fit:cover" />
        @Html.DisplayFor(modelItem => item.HINHANH)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.GIASANPHAM)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { @idch = item.IDCUAHANG, @idsp = item.IDSANPHAM, @idkc = item.IDKICHCO }) |
        @Html.ActionLink("Details", "Details", new { @idch = item.IDCUAHANG, @idsp = item.IDSANPHAM, @idkc = item.IDKICHCO }) |
        @Html.ActionLink("Delete", "Delete", new { @idch = item.IDCUAHANG, @idsp = item.IDSANPHAM, @idkc = item.IDKICHCO })
    </td>
</tr>
}

</table>
