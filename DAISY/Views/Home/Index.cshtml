@model IEnumerable<DAISY.Models.tb_SANPHAM>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2></h2>



<h4 style="margin-top:5rem">Trà sữa</h4>
<hr />


<div class="container">
    <div class="row row-cols-2 row-cols-lg-6 g-2 g-lg-3">
        @foreach (var item in Model)
        {
        <div class="col">
            <div class="card shadow" style=" border:none; border-radius: 1rem ">
                <img style="border-radius:1rem;" src="" class="card-img-top" alt="...">
                <div class="card-body">
                    <h6 class="card-title" style="height: 40px;">@Html.DisplayFor(modelItem => item.TENSANPHAM)</h6>
                    <h6 class="card-title" style="color:red">$<b>@String.Format("{0:0,0}", item.GIASANPHAM)₫</b></h6>
                    <div class="d-grid gap-2">
                        <button class="btn btn-outline-warning" type="button">Chọn mua</button>
                    </div>
                </div>
            </div>
            
        </div>
         }
    </div>
</div>
