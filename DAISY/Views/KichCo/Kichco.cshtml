@model IEnumerable<DAISY.Models.tb_KICHCO>

@{
    ViewBag.Title = "Kichco";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Các kích cỡ</h2>

<p>
    @Html.ActionLink("Thêm kích cỡ", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TENKICHCO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.THETICH_TRONGLUONG)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TENKICHCO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.THETICH_TRONGLUONG)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDKICHCO }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDKICHCO }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDKICHCO })
        </td>
    </tr>
}

</table>
