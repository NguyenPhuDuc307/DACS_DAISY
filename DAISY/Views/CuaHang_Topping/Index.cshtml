@model IEnumerable<DAISY.Models.tb_CUAHANG_SPDK>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tb_CUAHANG.TENCUAHANG)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TENSPDK)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HINHANH)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GIABAN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TRANGTHAI)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.tb_CUAHANG.TENCUAHANG)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TENSPDK)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.HINHANH)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.GIABAN)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.TRANGTHAI)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { @idch = item.IDCUAHANG, @idsp = item.IDSPDK }) |
        @Html.ActionLink("Details", "Details", new { @idch = item.IDCUAHANG, @idsp = item.IDSPDK }) |
        @Html.ActionLink("Delete", "Delete", new { @idch = item.IDCUAHANG, @idsp = item.IDSPDK })
    </td>
</tr>
}

</table>
